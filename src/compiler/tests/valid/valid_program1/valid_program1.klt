var a: Int = 6;
var b: Int;

fun max(num1: Int; num2: Int): Int {

    var res: Int;

    a = 10;
    b = 10;

    // TODO no funciona el or ||
    if (num1 < num2 && true) {
        res = num2;
    } else {
        res = ;
    }

    return res;
}

fun main (a: Int; b: Boolean) : None {
    var c: Int;
    var d: Int;
    var e: Int;

    c = 30 + 2 * 3;
    d = a / 3;
    e = max(a, b);

    print("El numero mas grande es...");
    print(c);
}
