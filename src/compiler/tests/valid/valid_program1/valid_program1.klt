var a: Int = 6;
var b: Int;

fun max(num1: Int; num2: Int): Int {

    var res: Int;

    a = 10;
    b = 10;

    if (num1 < num2) {
        res = num2;
    } else {
        res = num1;
    }

    return result;
}

fun main (a: Int; b: Boolean) : None {
    var c: Int;
    var d: Int;
    var e: Int;

    c = 30 + 2 * 3;
    d = a / 3;
    e = max(a, b);

    print("El numero mas grande es...");
    print(c);
}
