var a: Int;
var b: Int;

a = 10;
b = 10;

fun max(num1: Int; num2: Int): Int {
    var res: Int;
    if (num1 < num2) {
        res = num2;
    } else {
        res = num1;
    }

    return result;
}

fun main () : None {
    var a: Int;
    var b: Int;
    var c: Int;

    a = 30 + 2 * 3;
    b = a / 3;
    c = max(a, b);

    print("El numero mas grande es...")
    print(c);
}
