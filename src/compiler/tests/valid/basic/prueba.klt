var globalA: Int;
var globalB: Int = 2;
var globalC: Int;

fun sumar (num1: Int; num2: Int): Int {

    globalB = 3;
    var res: Int;
    res = num1 + num2;
    return 2 + 2;

}

fun main (): None {
    var comentario: String;

    globalA = 1;
    globalB = 2;
    globalC = sumar(globalA, globalB);

    comentario = "La suma es %d";
    print(comentario, globalC);
}